# Apex v1

Backend/service and platform setup for Apex v1.

## Structure

- `service/` application code
- `docs/` architecture and delivery docs
- `ci/` deployment scripts and trigger configs

## Environments

- dev
- staging
- prod

## CI/CD

Cloud Build trigger configs live in `service/cloudbuild/`.
