## Apex Player Create Flow (Drop 1 & 2)
- Delivered Drop 1 (player list + profile + query hooks) on branch `feat/player-create-flow`.
- Drop 2 added the PlayerGuardiansCard (link/unlink guardians) and the “Add player” CTA on the players list page.
- Currently executing Drop 3 (prototype polish + RTL render tests for CreatePlayerForm & PlayerGuardiansCard) targeting ~15:45 EDT.

